const fetchingWeather = 'Fetching weather from Openweather';
const errorFetchingWeather = 'Error fetching live weather from Openweather';
const creatingRecord = 'Creating new record';
const updatingRecord = 'Updating current record';

export const locale = {
  fetchingWeather,
  errorFetchingWeather,
  creatingRecord,
  updatingRecord
};
